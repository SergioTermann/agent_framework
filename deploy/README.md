# Deploy

Target home for deployment assets after migration.

Examples:

- service-specific Dockerfiles
- compose manifests
- k8s manifests
- environment templates

Current deploy assets remain at repository root until they are reorganized.
