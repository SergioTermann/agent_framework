# Frontend

Target home for UI assets once they are separated from backend source trees.

Planned subdirectories:

- `templates/`
- `static/`
- `optional-spa/`

Current sources of truth remain under `src/agent_framework/templates` and
`src/agent_framework/static`.
