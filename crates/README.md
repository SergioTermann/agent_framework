# Crates

This directory is the target home for Rust crates that own pure compute and
performance-sensitive kernels.

Expected crates:

- `vector-core`
- `retrieval-core`
- `causal-core`
- `ws-core`

Current sources of truth remain under `rust_extensions/` until they are moved.
