# retrieval-python

Keep Python here for:

- knowledge base orchestration
- retrieval pipeline composition
- adapters that still depend on Python model stacks

Do not move pure retrieval kernels here if they can live in Rust.

This service should call Rust kernels for performance-sensitive work instead
of re-implementing them in Python.
